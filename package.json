{
  "name": "web-resilience-profile",
  "version": "1.0.0",
  "description": "Web resilience test profile static site generator",
  "scripts": {
    "build": "node scripts/build-wrapper.js",
    "build:all": "node scripts/build.js --all && node scripts/build-worktree.js",
    "build-worktree": "node scripts/build-worktree.js",
    "deploy": "node scripts/deploy.js",
    "generate:sitemap": "node scripts/generate-sitemap.js --base https://resilience.ocf.tw/web/"
  },
  "dependencies": {
    "playwright": "^1.40.0"
  }
}
